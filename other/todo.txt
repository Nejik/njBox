* поповер вставлять в обертке, что-бы можно было делать любые анимации и позиционирование не мешало
* добавить возможность для поповера делать зеркальное отображение а не прилипание к краю
*попробовать избавиться от упоминания поповера в мэйн плагине
* role dialog вешать на wrap, а неактивные слайды прятать специальным аттрибутом

* выкосить глобальный неймспейс и аддоны из него
* ранить defaults в ссылке где-то в инстансе, что-бы был доступ елси нету njbox в глобальном контексте
* подумать как избавить от обращения к глобальным njBox....


* If there's no inherit modal heading element set for the modal's aria-labelledby attribute to reference, then the script will look for the first heading within the modal and set that to be the     modal's label source.
  aria label or aria-labelledby from header option
  role="alertdialog" http://www.scottohara.me/blog/2016/09/07/revised-modal-window.html

Docs:
* пример как сделать o.close inside
* в tips добавить подсказку что ширину/высоту можно через css ставить
* пример как открыть програмно без дом элементов
* рассказать про this.rawItems
* content может быть функцией
* сделать пример для max-height: calc(100vh - 100px - 20px);
* data_gathered, item_domready, item_prepare, item_inserted
* примеры как сделать триггер ивентов на дом элемент/документ
* пример static margin top https://github.com/dolce/iziModal/issues/68
* описать различия img ready/load
* сделать пример с методом update, для динамической галлереи
* preload в любом случае происходит на 1 слайд влево/вправо, а дополнительный можно настроить опцией preload
* пример с анимациями из animate.css
* про delayed content 
* сохраняем инстанс модального в дом элемент
* пример с oncb колбэком
* добавить примеры html api настроек
* потестить передачу jquery в тестовых методах, сейчас поидее оно не используется при инициализации
* координаты позиционирования можно передавать через аргумент .position, через o.placement (o.placement, самый сильный аргумент из o.position)
* в popover моде нельзя изменить o.container, он всегда будет body
* добавлен ивент positioned

Плюсы
адаптивное по умолчанию
центрировние с помощью css
Keeping track of the last focused element so that focus can return to it once the modal is closed.
Allowing for multiple options to close the modal window (close button, escape key, mouse click on overlay).
Trapping focus within the modal so that users can't tab out of it and interact with content that is obscured by the modal.